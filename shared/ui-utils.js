export const $=id=>document.getElementById(id);
export const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
export const fmt=n=>n==null||!Number.isFinite(+n)?'—':Number(n).toLocaleString(undefined,{maximumFractionDigits:2});
export const pct=n=>n==null||!Number.isFinite(+n)?'—':`${Number(n).toFixed(2)}%`;
export const statusPill=s=>`<span class="pill status-${String(s||'').toLowerCase().replace(/\s+/g,'-')}">${esc(s||'Unknown')}</span>`;
export function downloadCsv(name,rows){const csv=rows.map(r=>r.map(v=>`"${String(v??'').replace(/"/g,'""')}"`).join(',')).join('\r\n');const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([csv],{type:'text/csv;charset=utf-8'}));a.download=name;a.click();URL.revokeObjectURL(a.href)}
export async function api(path,options={}){const endpoint=String(path||'');if(!endpoint.startsWith('/'))throw new Error(`API endpoint must be relative: ${endpoint}`);const method=String(options.method||'GET').toUpperCase(),cacheKey=`mtd-api-cache:${endpoint}`;const r=await fetch(endpoint,{cache:'no-store',...options,headers:{'Content-Type':'application/json',...(options.headers||{})}});const text=await r.text();let data;try{data=text?JSON.parse(text):{}}catch{data={error:text}}if(!r.ok){const error=new Error(`Request to ${endpoint} returned HTTP ${r.status}${data.error?`: ${data.error}`:''}`);error.endpoint=endpoint;error.status=r.status;error.response=data;try{const cached=localStorage.getItem(cacheKey);if(cached)error.cachedResult=JSON.parse(cached)}catch{}throw error}if(method==='GET'){try{localStorage.setItem(cacheKey,JSON.stringify(data))}catch{}}return data}
export function setMessage(text,type='info'){const el=$('message');if(!el)return;el.className=`message ${type}`;el.textContent=text;el.hidden=!text}
