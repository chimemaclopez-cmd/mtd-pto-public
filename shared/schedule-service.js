import {api} from './ui-utils.js';
export const loadSchedules=()=>api('/api/schedules');
export const addSchedule=schedule=>api('/api/schedules',{method:'POST',body:JSON.stringify(schedule)});
export const loadSchedule=scheduleId=>api(`/api/schedules/${encodeURIComponent(scheduleId)}`);
export const updateSchedule=(scheduleId,schedule)=>api(`/api/schedules/${encodeURIComponent(scheduleId)}`,{method:'PUT',body:JSON.stringify(schedule)});
export const schedulesByDate=date=>api(`/api/schedules/by-date?date=${encodeURIComponent(date)}`);
export const schedulesByWeek=week=>api(`/api/schedules/by-week?week=${encodeURIComponent(week)}`);
export const loadScheduleHistory=()=>api('/api/schedules/history');
export const loadScheduleGroups=()=>api('/api/schedule-groups');
export const saveScheduleGroup=group=>api('/api/schedule-groups',{method:'POST',body:JSON.stringify(group)});
export const loadScheduleOverrides=(date='')=>api(`/api/schedule-overrides${date?`?date=${encodeURIComponent(date)}`:''}`);
export const saveScheduleOverride=override=>api('/api/schedule-overrides',{method:'POST',body:JSON.stringify(override)});
export const updateScheduleOverride=(id,override)=>api(`/api/schedule-overrides/${encodeURIComponent(id)}`,{method:'PUT',body:JSON.stringify(override)});
export const deleteScheduleOverride=id=>api(`/api/schedule-overrides/${encodeURIComponent(id)}`,{method:'DELETE'});
export const loadCoverage=date=>api(`/api/schedule-coverage?date=${encodeURIComponent(date)}`);
export const loadActivities=()=>api('/api/schedule-activities');
export const createActivity=activity=>api('/api/schedule-activities',{method:'POST',body:JSON.stringify(activity)});
export const updateActivity=(id,activity)=>api(`/api/schedule-activities/${encodeURIComponent(id)}`,{method:'PUT',body:JSON.stringify(activity)});
export const loadActivityBlocks=({employeeEmail='',scheduleId=''}={})=>api(`/api/schedule-activity-blocks?employeeEmail=${encodeURIComponent(employeeEmail)}&scheduleId=${encodeURIComponent(scheduleId)}`);
